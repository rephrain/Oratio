name: Deploy to NAS

on:
push:
branches:
- main

jobs:
deploy:
runs-on: self-hosted

```
steps:
  - name: Deploy
    run: |
      cd /share/CACHEDEV1_DATA/homes/oratio/oratio-ci
      sh deploy.sh
```